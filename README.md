# mentoring-farcaster-project
go to '/backend' folder and run: docker-compose up --build
then go to '/frontend' folder and run: npm install && npm start

# 로그인 기능(회원가입 + 로그인 + JWT 인증) 을 추가
npm install @nestjs/passport passport @nestjs/jwt passport-jwt bcrypt
npm install -D @types/passport-jwt @types/bcrypt
